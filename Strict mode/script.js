'use strict';

// Strict mode help us identify errors which don't get logged out generally in javascript

let hasdriversLicence = 'false';
const passTest = true;

if (passTest) hasdriverLicence = true;
if (hasdriversLicence) console.log("I can drive :D");